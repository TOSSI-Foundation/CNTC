"""O-CU-CP (gNB-CU-CP) suite, NGAP/N2, F1AP/F1-C, E1AP/E1, RRC and AS security.

Real cases land here as ``checks.py`` (module-level ``TESTS`` list) in phase P2, each verified
against the live stack before it counts as implemented. Until then the registry fills every
catalog requirement with a StubCase -> 'na'.
"""
