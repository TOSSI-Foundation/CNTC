"""O-DU (gNB-DU) suite, F1AP/F1-C from the DU side, the cell (MIB/SIB1, RACH) and F1-U.

Real cases land here as ``checks.py`` (module-level ``TESTS`` list) in phase P3, each verified
against the live stack before it counts as implemented. Until then the registry fills every
catalog requirement with a StubCase -> 'na'.
"""
