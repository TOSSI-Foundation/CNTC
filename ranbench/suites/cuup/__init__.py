"""O-CU-UP (gNB-CU-UP) suite, E1AP, F1-U and N3 GTP-U, and UP security policy enforcement.

Real cases land here as ``checks.py`` (module-level ``TESTS`` list) in phase P4, each verified
against the live stack before it counts as implemented. Until then the registry fills every
catalog requirement with a StubCase -> 'na'.
"""
